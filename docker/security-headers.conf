# Security headers applied to ALL responses, including redirects and error
# pages (where CI4's after-filter does not run). These are set ONLY here (not
# in the PHP filter) to avoid duplicates. CSP stays in the PHP filter since it
# needs the per-app CDN allowlist.
<IfModule mod_headers.c>
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"

    # Hide the PHP version banner.
    Header unset X-Powered-By
    Header always unset X-Powered-By
</IfModule>
